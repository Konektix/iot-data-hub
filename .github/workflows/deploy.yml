name: deploy

on: push

jobs:
  request-restart:
    runs-on: ubuntu-latest
    steps:
      - run: |
          repository=${{ github.repository }}
          repositoryName=${repository#Konektix/}
          
          curl -L \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer 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" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/Konektix/Cloud/actions/workflows/deploy.yml/dispatches \
            -d '{"ref":"main","inputs":{"service":"${repositoryName}"}}'
